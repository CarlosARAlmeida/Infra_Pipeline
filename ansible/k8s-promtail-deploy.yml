- name: Playbook para Deploy do Promtail no K3s
  hosts: k8s_master
  become: true
  tasks:
    - name: RBAC para o Promtail (Garantir permissões)
      kubernetes.core.k8s:
        kubeconfig: /etc/rancher/k3s/k3s.yaml
        state: present
        definition:
          - apiVersion: v1
            kind: ServiceAccount
            metadata:
              name: promtail-sa
              namespace: crud-lab
          - apiVersion: rbac.authorization.k8s.io/v1
            kind: ClusterRole
            metadata:
              name: promtail-clusterrole
            rules:
            - apiGroups: [""]
              resources: ["nodes", "nodes/proxy", "services", "endpoints", "pods"]
              verbs: ["get", "list", "watch"]
          - apiVersion: rbac.authorization.k8s.io/v1
            kind: ClusterRoleBinding
            metadata:
              name: promtail-clusterrolebinding
            subjects:
            - kind: ServiceAccount
              name: promtail-sa
              namespace: crud-lab
            roleRef:
              kind: ClusterRole
              name: promtail-clusterrole
              apiGroup: rbac.authorization.k8s.io

    - name: Criar ConfigMap do Promtail (Estratégia Globbing)
      kubernetes.core.k8s:
        kubeconfig: /etc/rancher/k3s/k3s.yaml
        state: present
        definition:
          apiVersion: v1
          kind: ConfigMap
          metadata:
            name: promtail-config
            namespace: crud-lab
          data:
            promtail.yaml: |
              server:
                http_listen_port: 9080
                grpc_listen_port: 0

              clients:
                - url: http://172.22.0.87:3100/loki/api/v1/push

              scrape_configs:
                - job_name: kubernetes-pods
                  kubernetes_sd_configs:
                    - role: pod
                  relabel_configs:
                    # 1. Filtra apenas o namespace de interesse
                    - source_labels: [__meta_kubernetes_namespace]
                      action: keep
                      regex: crud-lab

                    # 2. Labels para o Grafana
                    - source_labels: [__meta_kubernetes_pod_name]
                      target_label: pod
                    - source_labels: [__meta_kubernetes_container_name]
                      target_label: container

                    # 3. O MAPA (Simplificado): Usa apenas o UID para achar a pasta
                    # K3s no Debian usa: /var/log/pods/NS_POD_UID/CONTAINER/0.log
                    - source_labels: [__meta_kubernetes_pod_uid, __meta_kubernetes_container_name]
                      separator: /
                      action: replace
                      replacement: /var/log/pods/*$1/$2/*.log
                      target_label: __path__

    - name: Deploy do Promtail (DaemonSet como Root)
      kubernetes.core.k8s:
        kubeconfig: /etc/rancher/k3s/k3s.yaml
        state: present
        definition:
          apiVersion: apps/v1
          kind: DaemonSet
          metadata:
            name: promtail
            namespace: crud-lab
          spec:
            selector:
              matchLabels:
                name: promtail
            template:
              metadata:
                labels:
                  name: promtail
              spec:
                service_account_name: promtail-sa
                securityContext:
                  runAsUser: 0
                  runAsGroup: 0
                containers:
                - name: promtail
                  image: grafana/promtail:2.9.4
                  args:
                  - -config.file=/etc/promtail/promtail.yaml
                  securityContext:
                    privileged: true
                  volumeMounts:
                  - name: pods-logs
                    mountPath: /var/log/pods
                    readOnly: true
                  - name: config
                    mountPath: /etc/promtail
                volumes:
                - name: pods-logs
                  host_path:
                    path: /var/log/pods
                - name: config
                  config_map:
                    name: promtail-config